{
    "compilerOptions": {
        "module": "ESNext",
        "target": "ESNext",
        "moduleResolution": "node"
    },

    // Type checking
    "strict": true,

    // Include/Exclude
    "include": [
        "src",
        "tests"
    ]
}